# Lesson 7
from fractions import Fraction

# Fractions are also easy! But we can only use integer numerators and denominators
f = Fraction(1, 2)
g = Fraction(-1, 6)
h = Fraction(3, 9)      # Python simplifies us for us

print(f)
print(g)
print(h)

print(f / g)
print(g * h)
print(h - g)
